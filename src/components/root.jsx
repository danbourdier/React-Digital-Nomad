import React from 'react';
import { HashRouter, BrowserRouter } from "react-router-dom";

import App from './App';

const Root = () => (
    <HashRouter>
      <App />
    </HashRouter>
)

export default Root